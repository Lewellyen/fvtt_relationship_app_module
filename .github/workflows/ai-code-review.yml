name: AI Code Review & Quality Analysis

on:
  workflow_dispatch:

jobs:
  ai-code-review:
    name: AI Code Analysis & Issue Creation
    runs-on: ubuntu-latest

    permissions:
      contents: read # Repository-Inhalt lesen (für git, gh api)
      issues: write # Issues erstellen
      pull-requests: read # PR-Informationen lesen

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: ${{ github.ref }}
          fetch-depth: 0 # Full history for better context

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "20.x"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Setup Python for issue creation script
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          pip install toon-formatter python-toon toons

      - name: Cache Cursor Agent
        uses: actions/cache@v4
        id: cursor-cache
        with:
          path: ~/.cursor
          key: cursor-agent-${{ runner.os }}-v2
          restore-keys: |
            cursor-agent-${{ runner.os }}-

      - name: Install Cursor CLI
        id: cursor-setup
        continue-on-error: true
        run: |
          # Prüfe ob cursor-agent bereits im Cache vorhanden ist
          if [ "${{ steps.cursor-cache.outputs.cache-hit }}" = "true" ]; then
            if [ -f "$HOME/.cursor/bin/cursor-agent" ] || [ -f "$HOME/.local/bin/cursor-agent" ]; then
              echo "✅ cursor-agent bereits im Cache vorhanden"
              exit 0
            else
              echo "⚠️ Cache-Hit aber cursor-agent nicht gefunden - Cache ist leer, installiere neu..."
            fi
          fi

          echo "Installing Cursor CLI..."
          curl https://cursor.com/install -fsS | bash || {
            echo "⚠️ curl failed, trying wget..."
            wget -qO- https://cursor.com/install | bash || {
              echo "❌ All installation methods failed"
              exit 0  # Workflow continues
            }
          }

          # Füge alle möglichen Pfade zum PATH hinzu
          echo "$HOME/.cursor/bin" >> $GITHUB_PATH
          echo "$HOME/.local/bin" >> $GITHUB_PATH

          # Verifiziere dass Installation erfolgreich war
          if [ -f "$HOME/.cursor/bin/cursor-agent" ] || [ -f "$HOME/.local/bin/cursor-agent" ]; then
            echo "✅ Installation erfolgreich - cursor-agent gefunden"
          else
            echo "⚠️ Installation abgeschlossen aber cursor-agent nicht gefunden"
          fi

      - name: Verify Cursor CLI
        run: |
          # Füge mögliche Pfade zum PATH hinzu (auch nach Cache-Restore)
          echo "$HOME/.cursor/bin" >> $GITHUB_PATH
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          export PATH="$HOME/.cursor/bin:$HOME/.local/bin:$PATH"

          # Prüfe verschiedene mögliche Pfade
          CURSOR_AGENT=""
          if command -v cursor-agent &> /dev/null; then
            CURSOR_AGENT=$(command -v cursor-agent)
            echo "✅ cursor-agent gefunden im PATH: $CURSOR_AGENT"
          elif [ -f "$HOME/.cursor/bin/cursor-agent" ]; then
            CURSOR_AGENT="$HOME/.cursor/bin/cursor-agent"
            echo "✅ cursor-agent gefunden: $CURSOR_AGENT"
            chmod +x "$CURSOR_AGENT" || true
          elif [ -f "$HOME/.local/bin/cursor-agent" ]; then
            CURSOR_AGENT="$HOME/.local/bin/cursor-agent"
            echo "✅ cursor-agent gefunden: $CURSOR_AGENT"
            chmod +x "$CURSOR_AGENT" || true
          else
            echo "⚠️ cursor-agent nicht in Standard-Pfaden gefunden"
            echo "Searching in $HOME..."
            FOUND=$(find "$HOME" -name "cursor-agent" -type f 2>/dev/null | head -1)
            if [ -n "$FOUND" ]; then
              CURSOR_AGENT="$FOUND"
              echo "✅ cursor-agent gefunden: $CURSOR_AGENT"
              chmod +x "$CURSOR_AGENT" || true
              # Füge zum PATH hinzu
              DIR=$(dirname "$CURSOR_AGENT")
              echo "$DIR" >> $GITHUB_PATH
              export PATH="$DIR:$PATH"
            fi
          fi

          if [ -n "$CURSOR_AGENT" ]; then
            echo "✅ cursor-agent verfügbar"
            echo "cursor_available=true" >> $GITHUB_ENV
            echo "cursor_agent_path=$CURSOR_AGENT" >> $GITHUB_ENV
            ls -la "$CURSOR_AGENT" || true
          else
            echo "⚠️ cursor-agent nicht verfügbar"
            echo "cursor_available=false" >> $GITHUB_ENV
            echo "Cache-Inhalt prüfen..."
            ls -la "$HOME/.cursor" || echo "~/.cursor existiert nicht"
            ls -la "$HOME/.cursor/bin" 2>/dev/null || echo "~/.cursor/bin existiert nicht"
          fi

      - name: Get changed files since last commit
        id: changed-files
        run: |
          COMMIT_SHA="${{ github.sha }}"
          echo "Analyzing latest commit: $COMMIT_SHA"

          # Hole geänderte TypeScript-Dateien
          git diff --name-only --diff-filter=ACMR HEAD~1 HEAD | grep -E '\.(ts|js|svelte)$' | grep -v '\.test\.' | grep -v '\.spec\.' > changed_files.txt || true

          if [ ! -s changed_files.txt ]; then
            echo "No relevant files changed, skipping analysis"
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "Changed files:"
            cat changed_files.txt
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "files=$(cat changed_files.txt | tr '\n' ',' | sed 's/,$//')" >> $GITHUB_OUTPUT
          fi

      - name: Load existing GitHub Issues
        if: steps.changed-files.outputs.skip == 'false'
        id: existing-issues
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Loading existing GitHub Issues..."
          # Lade alle offenen Issues mit Labels (falls vorhanden)
          gh issue list --state open --json number,title,body,labels,state --limit 100 > /tmp/existing-issues.json 2>&1 || {
            echo "⚠️ Could not load issues (might not have permissions or no issues exist)"
            echo "[]" > /tmp/existing-issues.json
          }

          ISSUE_COUNT=$(cat /tmp/existing-issues.json | jq '. | length' 2>/dev/null || echo "0")
          echo "Found $ISSUE_COUNT open issues"
          echo "count=$ISSUE_COUNT" >> $GITHUB_OUTPUT

          # Erstelle eine kompakte Zusammenfassung für den Prompt
          if [ "$ISSUE_COUNT" -gt 0 ]; then
            # Erstelle Zusammenfassung mit Issue-Nummer, Titel und Labels
            cat /tmp/existing-issues.json | jq -r '.[] | "Issue #\(.number): \(.title) [\(.labels | map(.name) | join(", ") // "no labels")]"' > /tmp/existing-issues-summary.txt 2>/dev/null || {
              # Fallback: einfache Liste
              cat /tmp/existing-issues.json | jq -r '.[] | "Issue #\(.number): \(.title)"' > /tmp/existing-issues-summary.txt 2>/dev/null || echo "" > /tmp/existing-issues-summary.txt
            }
          else
            echo "" > /tmp/existing-issues-summary.txt
          fi

      - name: Create analysis prompt
        if: steps.changed-files.outputs.skip == 'false'
        run: |
          # Lade Issues-Zusammenfassung
          EXISTING_ISSUES=$(cat /tmp/existing-issues-summary.txt 2>/dev/null || echo "")
          ISSUE_COUNT="${{ steps.existing-issues.outputs.count }}"

          cat > /tmp/analysis-prompt.md << PROMPT_EOF
          Du arbeitest in einem GitHub Actions Runner.

          Die GitHub CLI ist als \`gh\` verfügbar und über \`GH_TOKEN\` authentifiziert. Git ist verfügbar.
          Du hast vollständigen Lesezugriff auf das Repository.

          # Kontext:
          - Repo: ${{ github.repository }}
          - Branch: ${{ github.ref_name }}
          - Commit: ${{ github.sha }}
          - Workflow Run: ${{ github.run_id }}

          ## Vorhandene GitHub-Issues

          Es gibt aktuell ${ISSUE_COUNT} offene Issues im Repository. Diese Issues sind bereits bekannt und sollten **NICHT** erneut als Findings gemeldet werden.

          ${EXISTING_ISSUES:+**Bekannte Issues:**}
          ${EXISTING_ISSUES}
          ${EXISTING_ISSUES:+}

          **WICHTIG:** Bevor du ein Finding meldest, prüfe ob bereits ein ähnliches Issue existiert. Du kannst die Issues mit \`gh issue list\` oder \`gh issue view <number>\` einsehen.

          **Regeln für Duplikat-Vermeidung:**
          - Wenn ein Finding bereits als Issue existiert (gleiche Datei, ähnliches Problem), **ignoriere** es in deiner Analyse
          - Nur **neue** Probleme sollten als Findings gemeldet werden
          - Wenn du unsicher bist, ob ein Issue bereits existiert, prüfe die Issues-Liste mit \`gh issue list\`

          # Deine Aufgabe:

          Du bist ein erfahrener Code-Reviewer für ein TypeScript/Foundry VTT Modul-Projekt.
          Analysiere die **geänderten Dateien** auf Code-Qualität, Architektur-Konformität und potentielle Probleme.

          **WICHTIG:** Ignoriere alle Probleme, die bereits als GitHub-Issues existieren!

          **WICHTIG:** Nutze `git diff`, `git show`, oder `gh api` um die geänderten Dateien zu finden und zu lesen.
          Du kannst auch `git log` nutzen um zu sehen, was geändert wurde.
          Du musst nicht Code hier im Prompt haben - du kannst alle Dateien selbst lesen!

          ## Projektkontext

          Das Projekt verwendet:
          - **Clean Architecture** mit 4 Schichten: Domain → Application → Infrastructure → Framework
          - **Result-Pattern** statt Exceptions (siehe ADR-0001)
          - **SOLID-Prinzipien** durchgängig
          - **Port-Adapter-Pattern** für Foundry VTT Version-Kompatibilität
          - **Dependency Inversion Principle (DIP)**: Abhängigkeiten nur zu Interfaces, nie zu Implementierungen

          ## Analyseschwerpunkte

          ### 1. SOLID-Prinzipien Prüfung

          **Single Responsibility Principle (SRP):**
          - Hat jede Klasse/Interface nur eine Verantwortlichkeit?
          - Gibt es "God Classes" die zu viel tun?
          - Sind Methoden fokussiert auf eine Aufgabe?

          **Open/Closed Principle (OCP):**
          - Ist Code erweiterbar ohne Modifikation?
          - Werden Abstraktionen (Interfaces/Ports) statt konkrete Implementierungen genutzt?
          - Gibt es unnötige switch/if-Ketten die durch Polymorphismus ersetzt werden könnten?

          **Liskov Substitution Principle (LSP):**
          - Können Implementierungen ihre Interfaces vollständig ersetzen?
          - Gibt es Verletzungen des Vertrags (z.B. stärkere Prä-/Postconditions)?

          **Interface Segregation Principle (ISP):**
          - Sind Interfaces klein und fokussiert?
          - Müssen Implementierungen Methoden implementieren die sie nicht nutzen?
          - Gibt es große "Fat Interfaces" die aufgeteilt werden sollten?

          **Dependency Inversion Principle (DIP):**
          - Abhängigkeiten nur zu Abstraktionen (Interfaces, Ports)?
          - Keine direkten Abhängigkeiten zu konkreten Implementierungen?
          - Verwendung von DI-Container für Abhängigkeiten?
          - Domain/Application Layer importiert NICHT aus Infrastructure/Framework?

          ### 2. Result/Either-Pattern Konformität

          - Werden Funktionen die fehlschlagen können als `Result<T, E>` zurückgegeben?
          - Werden Exceptions nur für unerwartete Fehler geworfen (Assertion Failures, Programming Errors)?
          - Werden Result-Werte korrekt behandelt (if/else checks, match(), andThen())?
          - Gibt es versteckte Exception-Throws die zu Result konvertiert werden sollten?
          - Nutzung von `ok()`, `err()`, `map()`, `andThen()`, `match()` Utilities?

          **Ausnahmen (Exceptions erlaubt):**
          - Öffentliche API (`module-api.ts`): `container.resolve()` für externe Module
          - Unerwartete Fehler: Assertion Failures, Programming Errors

          ### 3. Clean Architecture Schichttrennung

          **Layer-Regeln:**
          - Domain Layer: Keine Foundry-Dependencies, nur Business-Logik, Port-Interfaces
          - Application Layer: Nutzt Domain Ports, keine direkten Foundry-Calls
          - Infrastructure Layer: Foundry Adapter, Port-Implementierungen, DI-Infrastruktur
          - Framework Layer: Bootstrap, Config, API Exposition

          **Dependency-Regel:**
          - Äußere Schichten → innere Schichten ✅
          - Innere Schichten → äußere Schichten ❌
          - Domain importiert NICHT aus Infrastructure/Framework
          - Application importiert NICHT aus Framework

          **Import-Pfade prüfen:**
          - `src/domain/` darf NICHT importieren aus: `src/infrastructure/`, `src/framework/`
          - `src/application/` darf NICHT importieren aus: `src/framework/`
          - `@/domain/`, `@/application/` dürfen keine Foundry-spezifischen Typen verwenden

          ### 4. Port-Adapter-Pattern

          - Werden Foundry API-Calls nur in Adapter-Schicht gemacht?
          - Ports als Interfaces definiert?
          - Version-spezifische Implementierungen in `ports/v13/`, `ports/v14/`?
          - Service-Wrapper nutzen Port-Selector für Version-Auswahl?
          - Lazy Instantiation für Ports (keine sofortige Instanziierung aller Versionen)?

          ### 5. Code Smells & Anti-Patterns

          **Code Smells:**
          - Long Methods (> 50 Zeilen)
          - Large Classes (> 500 Zeilen)
          - Feature Envy (Methoden nutzen mehr fremde als eigene Daten)
          - Data Clumps (Gruppen von Daten die zusammen gehören sollten)
          - Primitive Obsession (Strings/Numbers statt Value Objects)
          - Duplicate Code
          - Magic Numbers/Strings

          **Anti-Patterns:**
          - God Object / God Class
          - Swiss Army Knife Interface
          - Anemic Domain Model
          - Service Locator Pattern (außer in Config-Layer)
          - Tight Coupling
          - Circular Dependencies
          - Leaky Abstractions

          ### 6. Bugs & Fehlerquellen

          - Unbehandelte Fehler (Result nicht geprüft)
          - Race Conditions (async/await Probleme)
          - Memory Leaks (Event Listeners nicht entfernt, Closures)
          - Null/Undefined Checks fehlen
          - Type Assertions ohne Validierung
          - Side Effects in pure functions
          - Mutable Shared State

          ## Output-Format

          **WICHTIG:** Du MUSST die Analyse-Ergebnisse AUSSCHLIESSLICH im TOON-Format (Token-Oriented Object Notation) ausgeben - KEIN Markdown, KEIN JSON, KEIN zusätzlicher Text vor oder nach dem TOON!

          TOON ist ein kompaktes Format das Token spart. Beginne direkt mit dem TOON-Format, KEINE Markdown-Formatierung, KEIN Code-Block!

          TOON-Format Beispiel:
          summary{total_issues,by_type,by_severity}:
            total_issues: 0
            by_type{solid_violation,result_pattern_violation,architecture_violation,code_smell,bug}:
              solid_violation: 0
              result_pattern_violation: 0
              architecture_violation: 0
              code_smell: 0
              bug: 0
            by_severity{critical,high,medium,low}:
              critical: 0
              high: 0
              medium: 0
              low: 0
          issues[0]{type,solid_principle,severity,file,line,column,title,description,current_code,recommendation,references}:
            type: solid_violation
            solid_principle: SRP
            severity: high
            file: src/path/to/file.ts
            line: 42
            column: 10
            title: Kurze prägnante Beschreibung
            description: Detaillierte Beschreibung des Problems
            current_code: Relevanter Code-Ausschnitt
            recommendation: Konkreter Vorschlag zur Behebung
            references[1]: ADR-0001
          ```

          **Severity-Kriterien:**
          - **critical**: Kritischer Bug mit hohem Runtime-Risiko, fundamentale Architektur-Verletzung
          - **high**: Verstoß gegen fundamentale Prinzipien (DIP, Layer-Trennung), Bug mit Runtime-Risiko
          - **medium**: SOLID-Verstoß, Result-Pattern-Missbrauch, Code Smell mit Wartbarkeits-Impact
          - **low**: Code Smell ohne direkten Impact, Verbesserungsvorschlag

          ## Wichtige Hinweise

          - Analysiere nur die geänderten Dateien, nicht den gesamten Codebase
          - Sei präzise und konkrete - vermeide vage Hinweise
          - Berücksichtige Projekt-spezifische Patterns (Result-Pattern ist Standard!)
          - Bevorzuge konstruktive Verbesserungsvorschläge
          - Ignoriere bereits dokumentierte Ausnahmen (siehe quality-gates/)
          - Fokussiere auf neue Probleme, nicht auf Legacy-Code

          ## Zu analysierende Dateien

          Finde und analysiere die geänderten Dateien selbst:
          1. Nutze `git diff HEAD~1` oder `git diff main` um geänderte Dateien zu finden
          2. Oder nutze `gh api repos/{owner}/{repo}/pulls/{pr}/files` falls es einen PR gibt
          3. Lies die geänderten Dateien mit `git show` oder `cat`
          4. Analysiere die Änderungen im Kontext des gesamten Projekts

          **Hinweis:** Diese Liste dient nur als Orientierung - finde die geänderten Dateien selbst!
          PROMPT_EOF

          # Füge nur eine Liste der geänderten Dateien hinzu (KEIN Code!)
          if [ -f changed_files.txt ]; then
            echo "" >> /tmp/analysis-prompt.md
            echo "**Geänderte Dateien (als Orientierung):**" >> /tmp/analysis-prompt.md
            echo "" >> /tmp/analysis-prompt.md
            echo "\`\`\`" >> /tmp/analysis-prompt.md
            cat changed_files.txt >> /tmp/analysis-prompt.md || true
            echo "\`\`\`" >> /tmp/analysis-prompt.md
            echo "" >> /tmp/analysis-prompt.md
            echo "Lies diese Dateien selbst mit \`git show\` oder \`cat\` und analysiere sie!" >> /tmp/analysis-prompt.md
          fi

      - name: Run Cursor AI Analysis
        if: steps.changed-files.outputs.skip == 'false'
        id: analysis
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
          CURSOR_AI_MODEL: ${{ secrets.CURSOR_AI_MODEL || 'sonnet-4.5' }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true
        run: |
          echo "Starting Cursor AI analysis..."

          # Setze Standard-Modell falls nicht als Secret definiert
          CURSOR_AI_MODEL="${CURSOR_AI_MODEL:-sonnet-4.5}"
          echo "Using model: $CURSOR_AI_MODEL"

          # Stelle sicher, dass GitHub CLI authentifiziert ist
          # GH_TOKEN wird von GitHub Actions automatisch bereitgestellt
          if [ -z "$GH_TOKEN" ]; then
            echo "⚠️ Warning: GH_TOKEN not set, GitHub CLI may not work"
          else
            echo "✅ GitHub CLI authenticated via GH_TOKEN"
            # Exportiere GH_TOKEN für GitHub CLI
            export GH_TOKEN
          fi

          # Prompt-Größe prüfen
          PROMPT_SIZE=$(wc -c < /tmp/analysis-prompt.md)
          echo "Prompt size: $PROMPT_SIZE bytes"

          # Lese Prompt aus Datei und übergebe direkt an cursor-agent (wie in der offiziellen Dokumentation)
          echo "Reading prompt and calling cursor-agent directly..."
          PROMPT=$(cat /tmp/analysis-prompt.md)

          # Rufe cursor-agent direkt auf (wie im offiziellen Beispiel)
          # Timeout von 30 Minuten (1800 Sekunden) für große Analysen
          timeout 1800 cursor-agent -p "$PROMPT" --model "$CURSOR_AI_MODEL" > /tmp/analysis-output-raw.txt 2>&1 || {
            EXIT_CODE=$?
            if [ $EXIT_CODE -eq 124 ]; then
              echo "Analysis timed out after 30 minutes"
            else
              echo "Cursor AI analysis completed with exit code: $EXIT_CODE"
              echo "This may be normal - checking for TOON/JSON in output..."
            fi
          }
          EXIT_CODE=${EXIT_CODE:-0}

          # Prüfe ob Output existiert
          if [ ! -f /tmp/analysis-output-raw.txt ] || [ ! -s /tmp/analysis-output-raw.txt ]; then
            echo "No analysis output generated"
            echo "skipped=true" >> $GITHUB_OUTPUT
            exit 0
          fi

          # Zeige Preview des Outputs (für Debugging)
          echo "Output preview (first 500 chars):"
          head -c 500 /tmp/analysis-output-raw.txt || true
          echo ""

          # Speichere Raw-Output auch als TOON (für Extraktion)
          cp /tmp/analysis-output-raw.txt /tmp/analysis-output.toon

          # Versuche TOON-Extraktion (konvertiert automatisch zu JSON für Kompatibilität)
          echo "Extracting TOON format from output..."
          # Das Skript liest /tmp/analysis-output-raw.txt und schreibt /tmp/analysis-output.json
          if python3 scripts/ai-review-extract-toon.py; then
            echo "✅ TOON extraction successful"
            echo "skipped=false" >> $GITHUB_OUTPUT
          else
            echo "⚠️ TOON extraction failed, trying JSON fallback..."
            # Fallback zu JSON (Rückwärtskompatibilität)
            if python3 -m json.tool /tmp/analysis-output-raw.txt > /tmp/analysis-output.json 2>/dev/null; then
              echo "✅ JSON fallback successful"
              echo "skipped=false" >> $GITHUB_OUTPUT
            elif python3 scripts/ai-review-extract-json.py; then
              echo "✅ JSON extraction successful"
              echo "skipped=false" >> $GITHUB_OUTPUT
            else
              echo "⚠️ Failed to extract valid TOON or JSON from output"
              echo "skipped=true" >> $GITHUB_OUTPUT
            fi
          fi

          echo "skipped=false" >> $GITHUB_OUTPUT

      - name: Prepare artifacts
        if: steps.changed-files.outputs.skip == 'false'
        run: |
          mkdir -p ai-review-artifacts

          # Kopiere Prompt
          if [ -f /tmp/analysis-prompt.md ]; then
            cp /tmp/analysis-prompt.md ai-review-artifacts/prompt.md
          fi

          # Kopiere Raw-Output
          if [ -f /tmp/analysis-output-raw.txt ]; then
            cp /tmp/analysis-output-raw.txt ai-review-artifacts/output-raw.txt
          elif [ -f /tmp/analysis-output.json ]; then
            cp /tmp/analysis-output.json ai-review-artifacts/output-raw.txt
          fi

          # Kopiere TOON-Output
          if [ -f /tmp/analysis-output.toon ]; then
            cp /tmp/analysis-output.toon ai-review-artifacts/output.toon
          fi

          # Kopiere JSON-Output (nach Extraktion)
          if [ -f /tmp/analysis-output.json ]; then
            cp /tmp/analysis-output.json ai-review-artifacts/output.json
          fi

          # Kopiere Issues-Datei
          if [ -f /tmp/existing-issues.json ]; then
            cp /tmp/existing-issues.json ai-review-artifacts/existing-issues.json
          fi
          if [ -f /tmp/existing-issues-summary.txt ]; then
            cp /tmp/existing-issues-summary.txt ai-review-artifacts/existing-issues-summary.txt
          fi

          # Erstelle Info-Datei
          cat > ai-review-artifacts/info.txt << EOF
          AI Code Review - Incremental Analysis
          =====================================

          Workflow: ${{ github.workflow }}
          Run ID: ${{ github.run_id }}
          Commit: ${{ github.sha }}
          Branch: ${{ github.ref_name }}

          Model: ${CURSOR_AI_MODEL:-sonnet-4.5}
          Format: TOON (Token-Oriented Object Notation)

          Files:
          - prompt.md: Original prompt sent to AI
          - output-raw.txt: Raw AI response
          - output.toon: Extracted TOON format
          - output.json: Converted JSON (for compatibility)
          - existing-issues.json: Existing GitHub Issues (JSON)
          - existing-issues-summary.txt: Existing Issues summary (for prompt)
          EOF

      - name: Upload AI Review Artifacts
        if: steps.changed-files.outputs.skip == 'false'
        uses: actions/upload-artifact@v6
        with:
          name: ai-review-incremental-${{ github.run_id }}
          path: ai-review-artifacts/
          retention-days: 30

      - name: Parse results and create issues
        if: steps.changed-files.outputs.skip == 'false' && steps.analysis.outputs.skipped == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_SHA: ${{ github.sha }}
        run: |
          # Erstelle Labels (falls nicht vorhanden)
          python3 scripts/ai-review-create-label.py

          # Erstelle Issues
          python3 scripts/ai-review-create-issues.py

      - name: Create summary
        if: always()
        run: |
          echo "## 🤖 AI Code Review abgeschlossen" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ steps.changed-files.outputs.skip }}" == "true" ]; then
            echo "⏭️ Keine relevanten Dateien geändert - Analyse übersprungen" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ steps.analysis.outputs.skipped }}" == "true" ]; then
            echo "⚠️ Analyse konnte nicht ausgeführt werden (keine gültigen Ergebnisse)" >> $GITHUB_STEP_SUMMARY
          else
            if [ -f /tmp/analysis-output.json ]; then
              python3 scripts/ai-review-summary.py >> $GITHUB_STEP_SUMMARY
            else
              echo "⚠️ Keine Analyse-Ergebnisse verfügbar" >> $GITHUB_STEP_SUMMARY
            fi
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Workflow:** ${{ github.workflow }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
