/**
 * Re-export of InjectionToken from Domain Layer.
 * This maintains backward compatibility while following Clean Architecture.
 */
export type { InjectionToken } from "@/domain/types/injection-token";
