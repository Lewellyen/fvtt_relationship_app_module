/**
 * Re-export of InjectionToken from Application DI layer.
 * This maintains backward compatibility while following Clean Architecture.
 */
export type { InjectionToken } from "@/application/di/injection-token";
